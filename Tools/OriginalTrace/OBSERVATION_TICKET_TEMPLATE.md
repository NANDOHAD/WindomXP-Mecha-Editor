# 原作EXE観測チケット

このテンプレートをコピーし、原作EXEへAttachする前に全項目を埋めます。1チケットで扱う疑問は1つだけです。

## 識別

- チケットID:
- 作成日:
- 挙動クラス:
- 対応GT:
- 状態: `未着手` / `観測中` / `終了` / `降格`

## 解決する疑問

- 1文の疑問:
- この値が決まると選べる実装候補:
- 観測しない場合の既定分類: `原作高確度` / `原作推定` / `Unity代替`

## 既存証拠

- 一次擬似コード関数・行:
- 実ANI/SPT:
- 既存first-mismatch:
- EXE / ANI / SPT hash:
- 一次資料だけで一意に決められない理由:

## 観測契約

- 種別: `アンカー観測` / `適合trace` / `診断ログ`
- 比較する処理段階:
- 単位・座標系:
- 最小フィールド:
- 必要最小tick／イベント窓:
- 入口関数・アドレス:
- 後段関数・アドレス:
- 対象機体ECXの確定方法:
- 入力方法と受理確認点:
- 生ビットを保存するfloat32項目:

## 実行前チェック

- [ ] `WindomXP_orig.exe`を通常起動した。
- [ ] EXE / ANI / SPT hashが契約と一致した。
- [ ] x32dbgを実行中プロセスへAttachした。
- [ ] Attach直後の残留BPが空である。
- [ ] 低頻度action入口で現在PIDの自機ECXを確定した。
- [ ] 自動入力を使う場合、action入口ログで入力受理を確認できる。

## 成功・停止条件

- 成功条件:
- 同一経路の既定反復回数: `2`
- 3回目を追加する条件: 値の不一致 / 新規ポインター・offset / 非決定性の疑い
- 強制停止条件: 2セッション連続で対象特定、入力受理、またはBP運用に失敗
- 停止後の処置: 一次資料へ戻る / `原作高確度`へ降格 / `Unity代替`へ降格
- スコープ外で見つかった疑問の送り先:

## 終了時チェック

- [ ] アンカー観測、適合trace、診断ログを混同せず分類した。
- [ ] 未観測値を0または推定値で補っていない。
- [ ] 採用値と除外値の理由を記録した。
- [ ] BPを撤去した。
- [ ] 原作を実行再開した。
- [ ] 実行中にデタッチした。
- [ ] 原作一致、原作高確度、原作推定、Unity代替の表記境界を更新した。

## 結果

- 採用した観測:
- 除外した観測:
- 選択した実装候補:
- 最終証拠レベル:
- 追加観測が必要になる条件:
